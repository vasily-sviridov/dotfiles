# Dotfiles

## Installation

We are using dotbot to set things up. Steps:

0. Decide what you want to install: comment out `run_dotbot 'steps/...'` that you don't need
1. Clone this repo with: `git clone https://github.com/vasily-sviridov/dotfiles dotfiles`
2. `cd dotfiles/`
3. Run: `./install`
